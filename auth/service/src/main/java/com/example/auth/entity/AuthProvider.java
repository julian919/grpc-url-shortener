package com.example.auth.entity;

/** Only EMAIL is wired today. A second value slots in here later without touching
 * Principal or Login's shape -- that's the whole reason Login is its own entity. */
public enum AuthProvider {
  EMAIL
}
