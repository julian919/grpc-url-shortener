<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>com.example</groupId>
    <artifactId>grpc-scaling-workshop</artifactId>
    <version>0.1.0-SNAPSHOT</version>
    <relativePath>../../pom.xml</relativePath>
  </parent>

  <artifactId>auth-service</artifactId>
  <name>auth-service</name>

  <dependencies>
    <!-- Its own domain's schema. -->
    <dependency>
      <groupId>com.example</groupId>
      <artifactId>auth-api</artifactId>
    </dependency>

    <!-- Inbound token verification + proto-declared permission enforcement, contributed by
         auto-configuration. Replaces the three security classes each service used to carry. -->
    <dependency>
      <groupId>com.example</groupId>
      <artifactId>commons-security</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-grpc-server</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <!--
      Spring Boot 4.x moved Flyway autoconfiguration behind its own dedicated starter,
      same pattern as spring-boot-starter-grpc-server — plain flyway-core on the
      classpath is no longer enough to trigger it (confirmed: without this, Hibernate's
      schema validation runs before Flyway ever gets a chance to migrate anything).
    -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-flyway</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    <!--
      Not used for its resource-server behavior here (auth-service has no
      protected RPCs of its own) — pulled in for the NimbusJwtEncoder/RSAKey
      machinery it brings transitively, to MINT tokens rather than validate
      them. shortener-service is the one that actually validates, in its own
      copy of this same starter.
    -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <!--
      StatusProto, which attaches a google.rpc.Status (carrying ErrorInfo details)
      to a gRPC error's trailers; see AuthExceptionAdvice. It also brings in
      proto-google-common-protos, where ErrorInfo itself lives. Already on the
      classpath transitively; declared because this module now uses it directly.
    -->
    <dependency>
      <groupId>io.grpc</groupId>
      <artifactId>grpc-protobuf</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
